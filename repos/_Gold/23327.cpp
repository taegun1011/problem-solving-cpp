#define _CRT_SECURE_NO_WARNINGS
#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define FASTIO ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);

#define ll long long

int main() {
	//구간 합 5번 문제와 유사!
	FASTIO

	int N, Q;
	cin >> N >> Q;

	vector<int> team(N + 1);

	for (int i = 1; i <= N; i++)
		cin >> team[i];

	//누적 합 생성 
	
}